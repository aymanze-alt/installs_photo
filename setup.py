from setuptools import setup, find_packages

setup(
    name="sales_order_installer",
    version="0.0.1",
    description="Controlled Sales Order attachment and installed status workflow",
    packages=find_packages(),
    include_package_data=True,
)
